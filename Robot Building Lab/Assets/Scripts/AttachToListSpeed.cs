using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttachToListSpeed : MonoBehaviour
{
    public void attach() 
    {
        //transform.root.GetComponent<RobotController>().AccelerationWheels.Add(gameObject.GetComponent<WheelCollider>());
    }
    public void detach()
    {
        //transform.root.GetComponent<RobotController>().AccelerationWheels.Remove(gameObject.GetComponent<WheelCollider>());
    }
}
