using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttachToListSteering : MonoBehaviour
{
    public void attach()
    {
        //transform.root.GetComponent<RobotController>().SteeringWheels.Add(gameObject.GetComponent<WheelCollider>());
    }
    public void detach()
    {
        //transform.root.GetComponent<RobotController>().SteeringWheels.Remove(gameObject.GetComponent<WheelCollider>());
    }
}
